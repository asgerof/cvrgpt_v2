# === Runtime mode ===
APP_ENV=dev           # dev|prod

# === Data provider selection ===
# Use 'erst' as the canonical, always-on provider in prod
DATA_PROVIDER=erst    # erst|fixture

# === ERST/Datafordeler credentials (replace with your actual values in non-dev) ===
ERST_CLIENT_ID=
ERST_CLIENT_SECRET=
ERST_AUTH_URL=
ERST_TOKEN_AUDIENCE=
ERST_API_BASE_URL=     # e.g., https://services.datafordeler.dk/...
ERST_CERT_PATH=        # optional, if mutual TLS is required
ERST_KEY_PATH=         # optional, if mutual TLS is required

# === Backend API key (frontend should NOT leak this in prod) ===
API_KEY=dev-key
